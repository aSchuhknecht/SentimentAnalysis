Neural sentiment classifier which classifies movie reviews as either positive or negative. A feed-forward neural network was used as the classifier. The neural net training and optimization code
is also included. 

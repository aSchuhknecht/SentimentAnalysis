Neural sentiment classifier which classifies sentences as either positive or negative. A feed-forward neural network was used as the classifier. The neural net training and optimization code
is also included. 
